EMAIL_ADDRESS=
EMAIL_PASSWORD=

IMAP_SERVER=imap.gmail.com
IMAP_PORT=993

# Nombre de mails non lus traités à chaque clic sur "Lancer l'agent" (1 = un seul
# mail par lancement, pour voir le détail de chaque analyse avant de passer au
# suivant). Mettre une valeur plus grande (ex: 10) pour retraiter un lot d'un coup.
EMAIL_BATCH_SIZE=1

GROQ_API_KEY=
